import { createTheme } from '@mui/material/styles';
export const theme = createTheme({
  spacing: 8,
  palette: {},
  typography: {},
  transitions:{
    create:()=>'...',
    duration:{short:250, standard:500}

  }
});
